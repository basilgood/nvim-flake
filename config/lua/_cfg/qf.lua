require('bqf').setup({
  auto_resize_height = true,
  preview = {
    auto_preview = false,
    winblend = 1,
  },
})

require('pqf').setup({
  show_multiple_lines = true,
  max_filename_length = 40,
})
